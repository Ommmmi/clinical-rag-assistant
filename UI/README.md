
  # Medical Chatbot UI

  This is a code bundle for Medical Chatbot UI. The original project is available at https://www.figma.com/design/uNaUhgB2whNnwSFABcUv8V/Medical-Chatbot-UI.

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the development server.
  