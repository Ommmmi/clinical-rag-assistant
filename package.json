{
  "name": "medical-rag-chatbot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "UI"
  ],
  "scripts": {
    "ui:dev": "npm run dev -w UI",
    "ui:build": "npm run build -w UI",
    "ui:install": "npm install -w UI"
  }
}
